{{#if @message}}
  <div class="alert alert-danger fade show" role="alert">
    <strong>
      {{@message}}
    </strong>
    <button
      type="button"
      class="close"
      data-dismiss="alert"
      aria-label="Close"
      {{on 'click' this.dismiss}}
    >
      <span aria-hidden="true">
        ×
      </span>
    </button>
  </div>
{{/if}}